# No default target: the workspace mixes a wasm crate and a native crate.
# Plugin: cargo build --release --target wasm32-wasip1 -p zellij-crew-plugin
# CLI:    cargo build --release --target x86_64-unknown-linux-musl -p zellij-crew-cli
# (or just use the Makefile)
