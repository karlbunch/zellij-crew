[package]
name = "zellij-crew"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Zellij plugin that auto-renames tabs with names from a configurable pool"

[lib]
crate-type = ["cdylib"]

[dependencies]
zellij-tile = "0.43"

[profile.release]
opt-level = "s"
lto = true
strip = true
